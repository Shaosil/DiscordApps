{
    "configurations": [
        {
            "name": "Attach to Docker",
            "type": "docker",
            "request": "attach",
            "platform": "netCore",
            "containerName": "shaosilbot",
            "sourceFileMap": {
                "/source": "${workspaceFolder}"
            }
        }
    ]
}